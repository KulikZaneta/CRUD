#DATABASE:
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/books?serverTimezone=Europe/Warsaw&useSSL=False&allowPublicKeyRetrieval=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
